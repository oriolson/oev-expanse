name: Deploy Pages

on:
  push:
    branches:
      - main
      - oriolson/personal-fishing-app
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: fishing-log/package-lock.json

      - name: Install dependencies
        working-directory: fishing-log
        run: npm ci

      - name: Build
        working-directory: fishing-log
        run: npm run build

      # Each microapp is published under its own path, matching the Vite base.
      - name: Assemble site
        run: |
          mkdir -p site/fishing-log
          cp -r fishing-log/dist/. site/fishing-log/

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
